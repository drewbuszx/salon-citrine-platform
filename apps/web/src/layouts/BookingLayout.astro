---
import "../styles/global.css";
import "../styles/fonts.css";
import { BUSINESS } from "@saloncitrine/shared";
import BookingSteps, {
  type BookingStepId,
} from "../components/BookingSteps.astro";
import SiteHeader from "../components/SiteHeader.astro";

interface Props {
  title: string;
  step: BookingStepId;
  showStylistStep?: boolean;
}

const { title, step, showStylistStep = false } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="robots" content="noindex" />
    <title>{title} · Book · {BUSINESS.name}</title>
    <script is:inline>
      (function () {
        var storageKey = "theme";
        var stored = localStorage.getItem(storageKey);
        var prefersDark = window.matchMedia("(prefers-color-scheme: dark)").matches;
        var theme =
          stored === "dark" || stored === "light"
            ? stored
            : prefersDark
              ? "dark"
              : "light";
        document.documentElement.setAttribute("data-theme", theme);
      })();
    </script>
  </head>
  <body>
    <div class="booking-shell">
      <SiteHeader />

      <main class="booking-main">
        <BookingSteps current={step} showStylist={showStylistStep} />
        <div class="booking-content">
          <slot />
        </div>
      </main>

      <footer class="booking-footer">
        <p>
          Questions? Call <a href={`tel:${BUSINESS.phoneE164}`}>{BUSINESS.phone}</a>
        </p>
      </footer>
    </div>
  </body>
</html>

<style>
  .booking-shell {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    padding-top: var(--header-height);
  }

  .booking-main {
    flex: 1;
    width: min(var(--max-width), 100%);
    margin: 0 auto;
    padding: var(--section-padding);
    padding-bottom: calc(var(--section-padding) + 1rem);
  }

  .booking-content {
    margin-top: 1.5rem;
    padding: clamp(1.5rem, 4vw, 2.5rem);
    background: var(--color-booking-panel-bg);
    border: 1px solid var(--color-border-subtle);
    box-shadow: 0 8px 40px var(--color-card-shadow);
  }

  .booking-footer {
    padding: 1rem var(--section-padding) 2rem;
    text-align: center;
    font-size: 0.9rem;
    color: var(--color-gray);
    background: var(--color-booking-panel-bg);
    border-top: 1px solid var(--color-border-subtle);
  }

  .booking-footer a {
    color: var(--color-text);
  }
</style>
