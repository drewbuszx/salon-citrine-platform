---
import TeamSidebarFilter from "./TeamSidebarFilter.astro";

interface Props {
  label: string;
  placeholder?: string;
  count?: number | string | null;
  open?: boolean;
}

const props = Astro.props;
---

<TeamSidebarFilter {...props}>
  <slot />
</TeamSidebarFilter>
