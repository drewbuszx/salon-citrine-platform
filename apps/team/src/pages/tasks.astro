---
import TeamLayout from "../layouts/TeamLayout.astro";
import TeamListLayout from "../components/TeamListLayout.astro";
import TeamSidebarNav from "../components/TeamSidebarNav.astro";
import UiScrollRow from "../components/ui/UiScrollRow.astro";
import { isSalonManager } from "../lib/auth";
import { PAGE_CONTEXT } from "../lib/page-context";
import { teamUrl } from "../lib/supabase-server";

const { supabase, staff } = Astro.locals;

if (!staff) {
  return Astro.redirect(teamUrl("/login?error=unlinked"));
}

const managerView = isSalonManager(staff);

const { data: staffRows } = await supabase
  .from("staff")
  .select("id, name")
  .order("name");

const staffOptions = staffRows ?? [];
---

<TeamLayout title="Tasks" activeNav="tasks" plain wide fullBleed>
  <div
    class="tasks-page"
    data-tasks-app
    data-api-base={teamUrl("/api/tasks")}
    data-routines-api-base={teamUrl("/api/tasks/routines")}
    data-manager={managerView ? "1" : "0"}
    data-current-staff-id={staff.id}
  >
    <TeamListLayout
      title={PAGE_CONTEXT.tasks.nav}
      contextTitle={PAGE_CONTEXT.tasks.title}
      contextSubtitle={PAGE_CONTEXT.tasks.subtitle}
      sidebarTitle="Views"
      contentWidth="operational"
    >
      {
        managerView ? (
          <button slot="headerActions" class="team-list-layout__btn-primary" type="button" data-task-create>
            <span class="tasks-page__action-long">Create task</span>
            <span class="tasks-page__action-short">+ New entry</span>
          </button>
        ) : null
      }

      <div slot="toolbar" class="tasks-page__toolbar" data-tasks-toolbar>
        {
          managerView ? (
            <button class="team-list-layout__btn-secondary" type="button" data-task-create-checklist>
              Create checklist
            </button>
          ) : null
        }
        <button class="team-list-layout__btn-secondary" type="button" data-task-browse-open>
          Browse open tasks
        </button>
      </div>

      <div class="tasks-page__sidebar-nav" data-tasks-nav slot="sidebarFilters">
      <TeamSidebarNav label="Task views">
        <button class="team-list-layout__sidebar-nav-btn is-active" type="button" data-view="my" aria-selected="true">
          <span>Assigned to me</span>
        </button>
        <button class="team-list-layout__sidebar-nav-btn" type="button" data-view="available" aria-selected="false">
          <span>For everyone</span>
        </button>
        <button
          class="team-list-layout__sidebar-nav-btn"
          type="button"
          data-view="attention"
          aria-selected="false"
          title="Active tasks already overdue or due within 24 hours"
        >
          <span>Needs attention</span>
          <span
            class="team-list-layout__sidebar-badge team-list-layout__sidebar-badge--attention"
            data-attention-badge
            hidden
            aria-label="Tasks needing attention"
          >0</span>
        </button>
        <button class="team-list-layout__sidebar-nav-btn" type="button" data-view="completed" aria-selected="false">
          <span>Completed</span>
        </button>
        {
          managerView ? (
            <button class="team-list-layout__sidebar-nav-btn" type="button" data-view="all" aria-selected="false">
              <span>All active</span>
            </button>
          ) : null
        }
      </TeamSidebarNav>

      <TeamSidebarNav label="Salon routines">
        <button
          class="team-list-layout__sidebar-nav-btn"
          type="button"
          data-view="routine-opening"
          aria-selected="false"
        >
          <span>Opening the salon</span>
          <span class="team-list-layout__sidebar-badge" data-routine-badge="opening" hidden>0/0</span>
        </button>
        <button
          class="team-list-layout__sidebar-nav-btn"
          type="button"
          data-view="routine-closing"
          aria-selected="false"
        >
          <span>Closing the salon</span>
          <span class="team-list-layout__sidebar-badge" data-routine-badge="closing" hidden>0/0</span>
        </button>
      </TeamSidebarNav>
      </div>

      <div class="tasks-page__content">
        <div class="tasks-page__primary">
          <div class="tasks-routines" data-tasks-routines aria-label="Salon routines">
            <button class="tasks-routines__card" type="button" data-view="routine-opening">
              <span class="tasks-routines__icon" aria-hidden="true">☀</span>
              <span class="tasks-routines__body">
                <span class="tasks-routines__title">Opening the salon</span>
                <span class="tasks-routines__meta" data-routine-progress="opening">Loading…</span>
              </span>
            </button>
            <button class="tasks-routines__card" type="button" data-view="routine-closing">
              <span class="tasks-routines__icon" aria-hidden="true">☾</span>
              <span class="tasks-routines__body">
                <span class="tasks-routines__title">Closing the salon</span>
                <span class="tasks-routines__meta" data-routine-progress="closing">Loading…</span>
              </span>
            </button>
          </div>

          <div class="tasks-summary" data-tasks-summary aria-label="Task summary" hidden>
            <button class="tasks-summary__card" type="button" data-summary-view="my">
              <span class="tasks-summary__value" data-summary-assigned>—</span>
              <span class="tasks-summary__label">Assigned to me</span>
            </button>
            <button class="tasks-summary__card" type="button" data-summary-view="available">
              <span class="tasks-summary__value" data-summary-open>—</span>
              <span class="tasks-summary__label">Open to everyone</span>
            </button>
            <button class="tasks-summary__card" type="button" data-summary-view="attention">
              <span class="tasks-summary__value" data-summary-attention>—</span>
              <span class="tasks-summary__label">Needs attention</span>
            </button>
            <button class="tasks-summary__card" type="button" data-summary-view="attention">
              <span class="tasks-summary__value" data-summary-due-today>—</span>
              <span class="tasks-summary__label">Due today</span>
            </button>
            <button class="tasks-summary__card" type="button" data-summary-view="completed">
              <span class="tasks-summary__value" data-summary-completed-week>—</span>
              <span class="tasks-summary__label">Completed this week</span>
            </button>
          </div>

          <div class="tasks-tabs" data-tasks-nav>
            <UiScrollRow label="Task views">
              <button class="tasks-tabs__btn is-active" type="button" data-view="my" aria-selected="true">
                <span class="tasks-tabs__long">Assigned to me</span>
                <span class="tasks-tabs__short">Mine</span>
              </button>
              <button class="tasks-tabs__btn" type="button" data-view="available" aria-selected="false">
                <span class="tasks-tabs__long">For everyone</span>
                <span class="tasks-tabs__short">Open</span>
              </button>
              <button
                class="tasks-tabs__btn"
                type="button"
                data-view="attention"
                aria-selected="false"
                title="Active tasks already overdue or due within 24 hours"
              >
                <span class="tasks-tabs__long">Needs attention</span>
                <span class="tasks-tabs__short">Urgent</span>
                <span
                  class="tasks-tabs__badge"
                  data-attention-badge
                  hidden
                  aria-label="Tasks needing attention"
                >0</span>
              </button>
              <button class="tasks-tabs__btn" type="button" data-view="completed" aria-selected="false">
                <span class="tasks-tabs__long">Completed</span>
                <span class="tasks-tabs__short">Done</span>
              </button>
              {
                managerView ? (
                  <button class="tasks-tabs__btn" type="button" data-view="all" aria-selected="false">
                    <span class="tasks-tabs__long">All active</span>
                    <span class="tasks-tabs__short">All</span>
                  </button>
                ) : null
              }
              <button class="tasks-tabs__btn tasks-tabs__btn--routine" type="button" data-view="routine-opening" aria-selected="false">
                <span class="tasks-tabs__long">Opening</span>
                <span class="tasks-tabs__short">Open</span>
              </button>
              <button class="tasks-tabs__btn tasks-tabs__btn--routine" type="button" data-view="routine-closing" aria-selected="false">
                <span class="tasks-tabs__long">Closing</span>
                <span class="tasks-tabs__short">Close</span>
              </button>
            </UiScrollRow>
          </div>

          <div class="notebook" aria-label="Task notebook">
            <div class="notebook__binding" aria-hidden="true">
              <span class="notebook__hole"></span>
              <span class="notebook__hole"></span>
              <span class="notebook__hole"></span>
            </div>

            <div class="notebook__paper">
              <p class="notice notice--error tasks-page__error" data-tasks-error hidden role="alert"></p>

              <div class="tasks-list" data-tasks-list aria-live="polite">
                <p class="notebook-empty">Loading tasks…</p>
              </div>
            </div>
          </div>
        </div>

        <aside class="tasks-page__secondary" data-tasks-panels aria-label="Task insights" hidden>
          <section class="tasks-panel" data-panel-due-soon hidden>
            <h2 class="tasks-panel__title">Due soon</h2>
            <ul class="tasks-panel__list" data-panel-due-soon-list></ul>
          </section>

          <section class="tasks-panel" data-panel-open-claim hidden>
            <h2 class="tasks-panel__title">Open to claim</h2>
            <ul class="tasks-panel__list" data-panel-open-claim-list></ul>
          </section>

          <section class="tasks-panel" data-panel-checklist hidden>
            <h2 class="tasks-panel__title">Team checklist progress</h2>
            <div class="tasks-panel__progress" data-panel-checklist-body></div>
          </section>

          <section class="tasks-panel" data-panel-activity hidden>
            <h2 class="tasks-panel__title">Recent activity</h2>
            <ul class="tasks-panel__list tasks-panel__list--activity" data-panel-activity-list></ul>
          </section>
        </aside>
      </div>
    </TeamListLayout>
  </div>

  <dialog class="tasks-modal" data-task-modal aria-labelledby="task-modal-title">
    <form class="tasks-modal__form" method="dialog" data-task-form>
      <header class="tasks-modal__header">
        <h2 id="task-modal-title" data-task-modal-title>New task</h2>
        <button class="tasks-modal__close" type="button" data-task-modal-close aria-label="Close">
          ×
        </button>
      </header>

      <div class="form-stack">
        <label class="form-label">
          Title
          <input
            class="form-input"
            name="title"
            type="text"
            maxlength="200"
            required
            placeholder="e.g. Restock towels at the color station"
          />
        </label>

        <label class="form-label">
          Description (optional)
          <textarea
            class="form-input"
            name="description"
            rows="3"
            maxlength="2000"
            placeholder="What done looks like — where, how many, anything to watch for"></textarea>
        </label>

        <label class="form-label">
          Due date (optional)
          <input class="form-input" name="due_at" type="datetime-local" />
          <span class="tasks-modal__hint">Salon time (Indianapolis). Due or overdue within 24 hours lands in Needs attention.</span>
        </label>

        <label class="form-label">
          Priority
          <select class="form-input" name="priority">
            <option value="low">Low — when there's a lull</option>
            <option value="normal" selected>Normal — today's flow</option>
            <option value="high">High — before it affects clients</option>
          </select>
        </label>

        <fieldset class="tasks-modal__fieldset">
          <legend class="label-meta">Assignment</legend>
          <label class="tasks-modal__radio">
            <input type="radio" name="assignment_type" value="open" checked />
            <span>Open to team — shows in <strong>For everyone</strong>, first to claim it owns it</span>
          </label>
          <label class="tasks-modal__radio">
            <input type="radio" name="assignment_type" value="assigned" />
            <span>Assign to specific team member(s)</span>
          </label>
        </fieldset>

        <div class="tasks-modal__assignees" data-assignee-picker hidden>
          <span class="label-meta">Team members</span>
          <div class="tasks-modal__staff-list">
            {
              staffOptions.map((member) => (
                <label class="tasks-modal__staff-option">
                  <input type="checkbox" name="assignee_ids" value={member.id} />
                  <span>{member.name}</span>
                </label>
              ))
            }
          </div>
        </div>
      </div>

      <footer class="tasks-modal__footer">
        <button class="team-list-layout__btn-secondary" type="button" data-task-modal-close>Discard</button>
        <button class="team-list-layout__btn-primary" type="submit" data-task-submit>Save task</button>
      </footer>
    </form>
  </dialog>

  <dialog class="tasks-modal tasks-modal--narrow" data-complete-modal aria-labelledby="complete-modal-title">
    <form class="tasks-modal__form" method="dialog" data-complete-form>
      <header class="tasks-modal__header">
        <h2 id="complete-modal-title">Mark complete</h2>
        <button class="tasks-modal__close" type="button" data-complete-modal-close aria-label="Close">
          ×
        </button>
      </header>

      <div class="form-stack">
        <p class="tasks-modal__lead" data-complete-task-title></p>
        <p class="tasks-modal__hint">
          This records you as the one who finished it, with the time, under Completed.
        </p>

        <label class="form-label">
          Completion notes (optional)
          <textarea
            class="form-input"
            name="completion_notes"
            rows="3"
            maxlength="1000"
            placeholder="e.g. Used the last case — added towels to the supply order"></textarea>
        </label>
      </div>

      <footer class="tasks-modal__footer">
        <button class="team-list-layout__btn-secondary" type="button" data-complete-modal-close>Not yet</button>
        <button class="team-list-layout__btn-primary" type="submit">Mark done</button>
      </footer>
    </form>
  </dialog>
</TeamLayout>

<script src="../scripts/tasks.ts"></script>

<style>
  .tasks-page {
    --notebook-line: clamp(2rem, 1.7rem + 0.6vw, 2.25rem);
    --notebook-margin: clamp(2.25rem, 1.9rem + 1vw, 2.75rem);
    --notebook-max-width: clamp(56rem, 48vw + 12rem, 68.75rem);
    --tasks-content-max: min(100%, 88rem);
    --notebook-paper: #fdfaf4;
    --notebook-paper-dark: #1e1c19;
    --notebook-rule: rgba(174, 148, 143, 0.32);
    --notebook-margin-line: rgba(196, 90, 78, 0.42);
    --notebook-glow-1: rgba(231, 172, 70, 0.06);
    --notebook-glow-2: rgba(141, 158, 136, 0.05);
    /*
      Sketch palette (final rendered alpha = this shared opacity × each
      path's own opacity attribute, since background layers can't be
      dimmed individually): warm grey-brown #8a7f72 pencil linework reads
      at ~0.09, dusty-rose #c4a5a5 stardust at ~0.08, citrine #c9a227
      facet hints at ~0.06 — all inside the 0.05–0.12 discipline.
    */
    --notebook-sketch-opacity: 0.09;
    --notebook-sketch-moon: url("data:image/svg+xml,%3Csvg viewBox='0 0 48 48' xmlns='http://www.w3.org/2000/svg' fill='none'%3E%3Cpath d='M30 7c-10 1-17 10-17 19 0 9 7 16 16 17-7-4-10-10-10-17 0-8 5-15 11-19z' stroke='%238a7f72' stroke-width='1.15' stroke-linecap='round' stroke-linejoin='round'/%3E%3Ccircle cx='35' cy='11' r='0.9' fill='%23c9a227' opacity='0.67'/%3E%3C/svg%3E");
    --notebook-sketch-moon-sm: url("data:image/svg+xml,%3Csvg viewBox='0 0 40 40' xmlns='http://www.w3.org/2000/svg' fill='none'%3E%3Cpath d='M26 6c-8 1-13 8-13 15 0 7 5 13 12 14-6-3-8-8-8-14 0-6 4-11 9-15z' stroke='%238a7f72' stroke-width='1' stroke-linecap='round'/%3E%3Ccircle cx='31' cy='9' r='0.7' fill='%23c9a227' opacity='0.6'/%3E%3C/svg%3E");
    --notebook-sketch-crystal: url("data:image/svg+xml,%3Csvg viewBox='0 0 80 80' xmlns='http://www.w3.org/2000/svg' fill='none'%3E%3Cpath d='M40 6l10 24-10 44-10-44z' stroke='%238a7f72' stroke-width='1.05' stroke-linejoin='round'/%3E%3Cpath d='M24 34l8-12 8 16 8-12 8 8' stroke='%23c9a227' stroke-width='0.9' stroke-linecap='round' stroke-linejoin='round' opacity='0.67'/%3E%3Cpath d='M18 54l10-10 12 14 10-10 12 10' stroke='%23c9a227' stroke-width='0.85' stroke-linecap='round' stroke-linejoin='round' opacity='0.5'/%3E%3Cpath d='M34 22l6 8-6 8-6-8z' stroke='%23c4a5a5' stroke-width='0.75' opacity='0.89'/%3E%3C/svg%3E");
    --notebook-sketch-stars: url("data:image/svg+xml,%3Csvg viewBox='0 0 28 56' xmlns='http://www.w3.org/2000/svg' fill='none'%3E%3Cpath d='M9 10l.6 1.8 1.8.6-1.8.6-.6 1.8-.6-1.8-1.8-.6 1.8-.6z' stroke='%23c4a5a5' stroke-width='0.55' stroke-linejoin='round' opacity='0.89'/%3E%3Cpath d='M17 30l.5 1.5 1.5.5-1.5.5-.5 1.5-.5-1.5-1.5-.5 1.5-.5z' stroke='%23c4a5a5' stroke-width='0.5' stroke-linejoin='round' opacity='0.6'/%3E%3Ccircle cx='11' cy='46' r='0.75' fill='%238a7f72' opacity='1'/%3E%3Cpath d='M20 48l.4 1.2 1.2.4-1.2.4-.4 1.2-.4-1.2-1.2-.4 1.2-.4z' stroke='%23c9a227' stroke-width='0.45' opacity='0.67'/%3E%3C/svg%3E");
    --notebook-sketch-herb: url("data:image/svg+xml,%3Csvg viewBox='0 0 60 100' xmlns='http://www.w3.org/2000/svg' fill='none'%3E%3Cpath d='M30 94c-1-20 2-38-1-58-1.3-8 .5-16 3-24' stroke='%238a7f72' stroke-width='1' stroke-linecap='round'/%3E%3Cpath d='M29 70c-6-2-11 1-14 6 5-1 10-3 14-6z' stroke='%238a7f72' stroke-width='0.9' stroke-linejoin='round'/%3E%3Cpath d='M30 68c6-3 11 0 14 5-5-1-10-2-14-5z' stroke='%238a7f72' stroke-width='0.9' stroke-linejoin='round'/%3E%3Cpath d='M27 46c-6-2-10 1-13 6 5-1 9-3 13-6z' stroke='%238a7f72' stroke-width='0.85' stroke-linejoin='round'/%3E%3Cpath d='M28 44c5-3 10 0 13 5-5-1-9-2-13-5z' stroke='%238a7f72' stroke-width='0.85' stroke-linejoin='round'/%3E%3Cpath d='M26 24c-5-2-9 1-11 5 4-1 8-2 11-5z' stroke='%238a7f72' stroke-width='0.75' stroke-linejoin='round'/%3E%3Cpath d='M27 22c4-3 8 0 11 4-4-1-8-1-11-4z' stroke='%238a7f72' stroke-width='0.75' stroke-linejoin='round'/%3E%3Ccircle cx='31' cy='6' r='1.4' fill='%238a7f72' opacity='0.9'/%3E%3Ccircle cx='27' cy='9' r='1.1' fill='%238a7f72' opacity='0.7'/%3E%3Ccircle cx='34' cy='9' r='1.1' fill='%238a7f72' opacity='0.7'/%3E%3C/svg%3E");
    /*
      Paper ink palette — always dark-on-cream. Do not use inverted global
      stone tokens here; dark mode only swaps these scoped variables.
    */
    --notebook-ink: var(--color-stone-800);
    --notebook-ink-muted: var(--color-stone-600, var(--color-stone-500));
    --notebook-ink-faint: var(--color-stone-500);
    --notebook-surface: var(--color-bg);
    --notebook-surface-muted: var(--color-stone-100);
    --notebook-surface-hover: var(--color-stone-50);
    --notebook-border: var(--color-stone-200);
    --notebook-border-strong: var(--color-stone-300);
  }

  /*
    One sheet of paper for the whole list. The paper grows vertically with the
    task list — its bottom edge only appears after the final task. Centered and
    constrained to a natural page proportion so it never spans the full viewport.
  */
  .notebook {
    position: relative;
    display: flex;
    flex-direction: column;
    width: 100%;
    max-width: var(--notebook-max-width);
    margin: clamp(0.75rem, 0.4rem + 1vw, 1.5rem) auto 0;
    border: 1px solid var(--color-border-subtle);
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow: 0 6px 24px var(--color-card-shadow);
  }

  .notebook__binding {
    position: relative;
    z-index: 1;
    display: flex;
    justify-content: center;
    align-items: center;
    gap: clamp(2.5rem, 12vw, 5rem);
    height: 1.35rem;
    padding: 0 1rem;
    background: var(--color-cream-deep);
    box-shadow: inset 0 -3px 5px -2px rgba(60, 45, 40, 0.12);
  }

  .notebook__hole {
    width: 0.6rem;
    height: 0.6rem;
    border-radius: 50%;
    background: var(--notebook-paper);
    box-shadow:
      inset 0 1px 2px rgba(0, 0, 0, 0.22),
      0 1px 0 rgba(255, 255, 255, 0.4);
  }

  [data-theme="dark"] .notebook__hole {
    background: var(--notebook-paper-dark);
    box-shadow: inset 0 1px 2px rgba(0, 0, 0, 0.5);
  }

  /*
    The ONE paper layer for the whole list: base cream, horizontal ruled lines
    on the --notebook-line grid, and a single red margin line. No stacked raster
    textures — task rows sit transparently on top of this single surface.
  */
  .notebook__paper {
    position: relative;
    flex: 1 1 auto;
    min-height: 6rem;
    padding:
      calc(var(--notebook-line) * 0.65)
      2rem
      calc(var(--notebook-line) * 0.75)
      calc(var(--notebook-margin) + 1.75rem);
    background-color: var(--notebook-paper);
    background-repeat: repeat-y, no-repeat;
    background-size: 100% var(--notebook-line), 1.5px 100%;
    background-position: 0 0, var(--notebook-margin) 0;
    background-image:
      linear-gradient(
        to bottom,
        transparent calc(var(--notebook-line) - 1px),
        var(--notebook-rule) calc(var(--notebook-line) - 1px),
        var(--notebook-rule) var(--notebook-line)
      ),
      linear-gradient(var(--notebook-margin-line), var(--notebook-margin-line));
    isolation: isolate;
  }

  /*
    The single decorative layer — sketch motifs live only on the paper
    container, never on individual task rows. Corner/margin-anchored (no
    full-cover tiling) so nothing bands or reads as a second stacked sheet.
  */
  .notebook__paper::before {
    content: "";
    position: absolute;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    opacity: var(--notebook-sketch-opacity);
    background-repeat: no-repeat;
    background-size:
      3.25rem 3.25rem,
      5.5rem 5.5rem,
      3.75rem 6.25rem,
      1.75rem 3.5rem;
    background-position:
      calc(var(--notebook-margin) * 0.5) 0.75rem,
      right 0.85rem bottom 1rem,
      left 0.2rem bottom 0.75rem,
      right 0.6rem top 2.5rem;
    background-image:
      var(--notebook-sketch-moon),
      var(--notebook-sketch-crystal),
      var(--notebook-sketch-herb),
      var(--notebook-sketch-stars);
  }

  [data-theme="dark"] .notebook__paper {
    background-color: var(--notebook-paper-dark);
    --notebook-rule: rgba(90, 78, 68, 0.24);
    --notebook-margin-line: rgba(160, 72, 62, 0.44);
    --notebook-sketch-opacity: 0.08;
  }

  /*
    Dark shell + cream paper island: swap scoped ink variables only — notebook
    entry rules below read these and never inherit inverted global stone tokens.
  */
  [data-theme="dark"] .tasks-page {
    --notebook-paper-dark: #e8e0d2;
    --notebook-ink: #2f2a28;
    --notebook-ink-muted: #5c554e;
    --notebook-ink-faint: #787068;
    --notebook-surface: #f3f0ea;
    --notebook-surface-muted: #f3f1ec;
    --notebook-surface-hover: #faf9f7;
    --notebook-border: #d4cfc4;
    --notebook-border-strong: #a8a093;
  }

  .notebook__paper::after {
    content: "";
    position: absolute;
    inset: 0;
    z-index: 0;
    background-image:
      radial-gradient(ellipse 80% 45% at 12% 0%, var(--notebook-glow-1) 0%, transparent 60%),
      radial-gradient(ellipse 55% 35% at 92% 100%, var(--notebook-glow-2) 0%, transparent 55%);
    opacity: 0.6;
    pointer-events: none;
  }

  [data-theme="dark"] .notebook__paper::after {
    --notebook-glow-1: rgba(231, 172, 70, 0.05);
    --notebook-glow-2: rgba(141, 158, 136, 0.04);
    opacity: 0.4;
  }

  .tasks-page__action-long {
    display: none;
  }

  .tasks-page__action-short {
    display: inline;
  }

  .tasks-page__toolbar {
    display: none;
    flex-wrap: wrap;
    align-items: center;
    gap: var(--space-2);
    width: 100%;
    max-width: var(--tasks-content-max);
    margin-inline: auto;
  }

  .tasks-page__content {
    width: 100%;
    max-width: var(--tasks-content-max);
    margin-inline: auto;
  }

  .tasks-page__primary {
    min-width: 0;
  }

  .tasks-page__secondary {
    display: none;
    flex-direction: column;
    gap: 0.75rem;
    min-width: 0;
  }

  .tasks-routines {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.55rem;
    margin-bottom: 0.85rem;
  }

  .tasks-routines__card {
    display: flex;
    align-items: flex-start;
    gap: 0.55rem;
    min-height: 2.75rem;
    padding: 0.55rem 0.65rem;
    border: 1px solid var(--color-stone-200);
    border-radius: var(--radius-md);
    background: var(--color-bg);
    color: inherit;
    font: inherit;
    text-align: left;
    cursor: pointer;
    transition:
      border-color var(--transition-fast),
      background var(--transition-fast),
      box-shadow var(--transition-fast);
  }

  .tasks-routines__card:hover,
  .tasks-routines__card:focus-visible,
  .tasks-routines__card.is-active {
    border-color: var(--color-citrine);
    background: rgba(231, 172, 70, 0.08);
    outline: none;
    box-shadow: 0 0 0 2px var(--color-focus-ring);
  }

  .tasks-routines__icon {
    flex-shrink: 0;
    width: 1.75rem;
    height: 1.75rem;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 999px;
    background: rgba(231, 172, 70, 0.14);
    font-size: 0.95rem;
    line-height: 1;
  }

  .tasks-routines__body {
    display: grid;
    gap: 0.1rem;
    min-width: 0;
  }

  .tasks-routines__title {
    font-family: var(--font-body);
    font-size: 0.8125rem;
    font-weight: 600;
    line-height: 1.25;
    color: var(--color-stone-800);
  }

  .tasks-routines__meta {
    font-family: var(--font-body);
    font-size: 0.6875rem;
    line-height: 1.3;
    color: var(--color-stone-500);
    font-variant-numeric: tabular-nums;
  }

  .tasks-routines__meta.is-complete {
    color: #4f6b4a;
  }

  .tasks-page :global(.routine-checklist) {
    display: grid;
    gap: 0;
  }

  .tasks-page :global(.routine-checklist__header) {
    display: grid;
    gap: 0.35rem;
    margin-bottom: calc(var(--notebook-line) * 0.35);
    padding-bottom: calc(var(--notebook-line) * 0.25);
    border-bottom: 1px dashed var(--notebook-rule);
  }

  .tasks-page :global(.routine-checklist__title) {
    margin: 0;
    font-family: var(--font-display);
    font-size: var(--text-xl);
    font-weight: 500;
    line-height: 1.25;
    color: var(--notebook-ink);
  }

  .tasks-page :global(.routine-checklist__lead) {
    margin: 0;
    font-family: var(--font-body);
    font-size: var(--text-sm);
    line-height: 1.45;
    color: var(--notebook-ink-muted);
  }

  .tasks-page :global(.routine-checklist__progress) {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.45rem 0.75rem;
    font-family: var(--font-body);
    font-size: var(--text-xs);
    color: var(--notebook-ink-faint);
  }

  .tasks-page :global(.routine-checklist__progress-bar) {
    flex: 1 1 8rem;
    height: 0.4rem;
    border-radius: 999px;
    background: var(--notebook-surface-muted);
    overflow: hidden;
  }

  .tasks-page :global(.routine-checklist__progress-fill) {
    display: block;
    height: 100%;
    border-radius: inherit;
    background: var(--color-citrine);
    transition: width 0.2s ease;
  }

  .tasks-page :global(.routine-item) {
    display: flex;
    align-items: flex-start;
    gap: 0.65rem;
    padding: calc(var(--notebook-line) * 0.22) 0;
    min-height: 2.75rem;
  }

  .tasks-page :global(.routine-item + .routine-item) {
    border-top: 1px dashed var(--notebook-rule);
  }

  .tasks-page :global(.routine-item__toggle) {
    flex-shrink: 0;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2.75rem;
    height: 2.75rem;
    margin: -0.55rem 0;
    padding: 0;
    border: none;
    background: transparent;
    cursor: pointer;
  }

  .tasks-page :global(.routine-item__check) {
    width: 1.25rem;
    height: 1.25rem;
    border: 1.5px solid var(--color-dusty-rose);
    border-radius: 3px 4px 3px 2px;
    background: transparent;
    position: relative;
    transform: rotate(-1.5deg);
    transition:
      border-color 0.2s ease,
      background 0.2s ease;
  }

  .tasks-page :global(.routine-item.is-done .routine-item__check) {
    border-color: var(--color-sage);
    background: rgba(141, 158, 136, 0.12);
  }

  .tasks-page :global(.routine-item.is-done .routine-item__check::after) {
    content: "";
    position: absolute;
    left: 0.28rem;
    top: 0.08rem;
    width: 0.45rem;
    height: 0.7rem;
    border: solid 2px var(--color-sage);
    border-width: 0 2px 2px 0;
    transform: rotate(38deg);
  }

  .tasks-page :global(.routine-item__body) {
    flex: 1;
    min-width: 0;
    padding-top: 0.35rem;
  }

  .tasks-page :global(.routine-item__label) {
    margin: 0;
    font-family: var(--font-body);
    font-size: var(--text-base);
    line-height: 1.4;
    color: var(--notebook-ink);
  }

  .tasks-page :global(.routine-item.is-done .routine-item__label) {
    text-decoration: line-through;
    text-decoration-color: var(--color-sage);
    color: var(--notebook-ink-faint);
  }

  .tasks-page :global(.routine-item__meta) {
    margin: 0.15rem 0 0;
    font-family: var(--font-body);
    font-size: var(--text-xs);
    line-height: 1.35;
    color: var(--notebook-ink-faint);
  }

  .tasks-tabs__btn--routine {
    border-style: dashed;
  }

  @media (min-width: 901px) {
    .tasks-routines {
      display: none;
    }
  }

  .tasks-summary {
    display: none;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-bottom: 0.85rem;
  }

  .tasks-summary__card {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 0.1rem;
    min-width: 6.5rem;
    flex: 1 1 6.5rem;
    max-width: 9.5rem;
    padding: 0.45rem 0.65rem;
    border: 1px solid var(--color-stone-200);
    border-radius: var(--radius-sm);
    background: var(--color-bg);
    color: inherit;
    font: inherit;
    text-align: left;
    cursor: pointer;
    transition:
      border-color var(--transition-fast),
      background var(--transition-fast),
      box-shadow var(--transition-fast);
  }

  .tasks-summary__card:hover,
  .tasks-summary__card:focus-visible {
    border-color: var(--color-sage);
    background: var(--color-selection-bg, rgba(141, 158, 136, 0.08));
    outline: none;
    box-shadow: 0 0 0 2px var(--color-focus-ring);
  }

  .tasks-summary__value {
    font-family: var(--font-body);
    font-size: 1.05rem;
    font-weight: 600;
    font-variant-numeric: tabular-nums;
    line-height: 1.2;
    color: var(--color-stone-800);
  }

  .tasks-summary__label {
    font-family: var(--font-body);
    font-size: 0.68rem;
    font-weight: 500;
    line-height: 1.25;
    color: var(--color-stone-500);
  }

  .tasks-panel {
    padding: 0.75rem 0.85rem;
    border: 1px solid var(--color-stone-200);
    border-radius: var(--radius-md);
    background: var(--color-bg);
  }

  .tasks-panel__title {
    margin: 0 0 0.55rem;
    font-family: var(--font-body);
    font-size: 0.65rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--color-stone-500);
  }

  .tasks-panel__list {
    margin: 0;
    padding: 0;
    list-style: none;
    display: grid;
    gap: 0.35rem;
  }

  .tasks-panel__item {
    display: grid;
    gap: 0.1rem;
    padding: 0.35rem 0;
    border-top: 1px solid var(--color-stone-100);
  }

  .tasks-panel__item:first-child {
    border-top: none;
    padding-top: 0;
  }

  .tasks-panel__item-title {
    margin: 0;
    font-family: var(--font-body);
    font-size: 0.8125rem;
    font-weight: 500;
    line-height: 1.35;
    color: var(--color-stone-800);
  }

  .tasks-panel__item-meta {
    margin: 0;
    font-family: var(--font-body);
    font-size: 0.6875rem;
    line-height: 1.35;
    color: var(--color-stone-500);
  }

  .tasks-panel__empty {
    margin: 0;
    font-family: var(--font-body);
    font-size: 0.75rem;
    line-height: 1.4;
    color: var(--color-stone-500);
  }

  .tasks-panel__progress {
    display: grid;
    gap: 0.35rem;
  }

  .tasks-panel__progress-row {
    display: flex;
    justify-content: space-between;
    gap: 0.5rem;
    font-family: var(--font-body);
    font-size: 0.8125rem;
    line-height: 1.35;
    color: var(--color-stone-700);
  }

  .tasks-panel__progress-bar {
    height: 0.35rem;
    border-radius: 999px;
    background: var(--color-stone-100);
    overflow: hidden;
  }

  .tasks-panel__progress-fill {
    display: block;
    height: 100%;
    border-radius: inherit;
    background: var(--color-sage);
  }

  .tasks-tabs {
    display: none;
    max-width: var(--notebook-max-width);
    margin: 0 auto 0.65rem;
    min-width: 0;
  }

  .tasks-tabs__btn {
    display: inline-flex;
    flex: 0 0 auto;
    align-items: center;
    gap: 0.35rem;
    min-height: 2.25rem;
    padding: 0.3rem 0.7rem;
    border: 1px solid var(--color-stone-200);
    border-radius: 999px;
    background: var(--color-bg);
    color: var(--color-stone-600);
    font-family: var(--font-body);
    font-size: 0.8125rem;
    font-weight: 500;
    letter-spacing: normal;
    text-transform: none;
    white-space: nowrap;
    cursor: pointer;
    transition:
      background var(--transition-fast),
      border-color var(--transition-fast),
      color var(--transition-fast);
  }

  .tasks-tabs__btn:hover,
  .tasks-tabs__btn:focus-visible {
    color: var(--color-text);
    border-color: var(--color-stone-300);
    outline: none;
  }

  .tasks-tabs__btn.is-active {
    color: var(--color-text);
    border-color: var(--color-sage);
    background: var(--color-selection-bg, rgba(141, 158, 136, 0.12));
    box-shadow: inset 0 0 0 1px var(--color-sage);
  }

  .tasks-tabs__short {
    display: none;
  }

  .tasks-tabs__badge {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 1.15rem;
    height: 1.15rem;
    padding: 0 0.3rem;
    border-radius: 999px;
    background: var(--color-citrine);
    color: var(--color-black);
    font-size: 0.625rem;
    font-weight: 700;
    line-height: 1;
  }

  .tasks-tabs__badge[hidden] {
    display: none;
  }

  /* Context header aligns with notebook column */
  .tasks-page :global(.page-context-header) {
    max-width: var(--tasks-content-max);
    margin-inline: auto;
    width: 100%;
  }

  .tasks-page :global(.team-list-layout__toolbar) {
    display: none;
    max-width: var(--tasks-content-max);
    margin-inline: auto;
  }

  .tasks-page__error {
    position: relative;
    z-index: 1;
    margin-top: 0;
    margin-bottom: 0.75rem;
  }

  .tasks-list {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
  }

  .tasks-page :global(.notebook-empty),
  .tasks-page :global(.tasks-loading) {
    margin: 0;
    padding: var(--mobile-space-sm) 0;
    min-height: auto;
    font-family: var(--font-body);
    font-size: var(--text-sm);
    color: var(--notebook-ink-faint);
    line-height: var(--leading-normal);
  }

  .tasks-page :global(.tasks-loading--error) {
    color: var(--color-error, #b42318);
  }

  .tasks-page :global(.tasks-empty.ui-empty) {
    justify-items: start;
    margin: 0;
    padding: var(--mobile-space-md) 0;
    border: none;
    border-radius: 0;
    background: transparent;
    text-align: left;
  }

  .tasks-page :global(.tasks-empty .ui-empty__icon) {
    width: 2.25rem;
    height: 2.25rem;
    font-size: 1rem;
  }

  .tasks-page :global(.tasks-empty .ui-empty__title) {
    font-family: var(--font-body);
    font-size: var(--text-sm);
    font-weight: 600;
    color: var(--notebook-ink);
  }

  .tasks-page :global(.tasks-empty .ui-empty__hint) {
    font-size: var(--text-xs);
    max-width: none;
    color: var(--notebook-ink-faint);
  }

  .tasks-page :global(.tasks-empty .ui-empty__actions) {
    justify-content: flex-start;
    margin-top: 0.15rem;
  }

  /*
    Task rows are written directly on the paper: transparent, no card
    background, no box-shadow, no rounded corners, no per-row texture. Vertical
    rhythm follows the --notebook-line grid.
  */
  .tasks-page :global(.notebook-entry) {
    position: relative;
    background: transparent;
    padding: calc(var(--notebook-line) * 0.28) 0;
    border: none;
    border-radius: 0;
    box-shadow: none;
  }

  .tasks-page :global(.notebook-entry.is-active .notebook-entry__title) {
    color: var(--notebook-ink);
  }

  .tasks-page :global(.notebook-entry.is-active .notebook-entry__check) {
    border-color: var(--color-citrine-dark);
  }

  .tasks-page :global(.notebook-entry.is-done) {
    opacity: 0.82;
  }

  /* Subtle dashed separator between tasks — not a strong sheet-edge border. */
  .tasks-page :global(.notebook-entry + .notebook-entry) {
    margin-top: calc(var(--notebook-line) * 0.28);
    border-top: 1px dashed var(--notebook-rule);
    padding-top: calc(var(--notebook-line) * 0.5);
  }

  .tasks-page :global(.notebook-entry__row) {
    display: flex;
    align-items: flex-start;
    gap: 0.65rem;
    min-height: var(--notebook-line);
    line-height: var(--notebook-line);
  }

  .tasks-page :global(.notebook-entry__check) {
    flex-shrink: 0;
    width: 1.15rem;
    height: 1.15rem;
    margin-top: calc((var(--notebook-line) - 1.15rem) / 2);
    border: 1.5px solid var(--color-dusty-rose);
    border-radius: 3px 4px 3px 2px;
    background: transparent;
    position: relative;
    transform: rotate(-1.5deg);
    transition: border-color 0.2s ease;
  }

  .tasks-page :global(.notebook-entry.is-done .notebook-entry__check) {
    border-color: var(--color-sage);
    background: rgba(141, 158, 136, 0.12);
  }

  .tasks-page :global(.notebook-entry.is-done .notebook-entry__check::after) {
    content: "";
    position: absolute;
    left: 0.22rem;
    top: 0.05rem;
    width: 0.45rem;
    height: 0.7rem;
    border: solid 2px var(--color-sage);
    border-width: 0 2px 2px 0;
    transform: rotate(38deg);
  }

  .tasks-page :global(.notebook-entry__body) {
    flex: 1;
    min-width: 0;
    padding-bottom: calc(var(--notebook-line) * 0.1);
  }

  .tasks-page :global(.notebook-entry__main) {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.4rem 0.6rem;
    min-height: var(--notebook-line);
  }

  .tasks-page :global(.notebook-entry__title) {
    margin: 0;
    font-family: var(--font-display);
    font-size: var(--text-lg);
    font-weight: 500;
    line-height: var(--notebook-line);
    letter-spacing: 0.01em;
    color: var(--notebook-ink);
  }

  .tasks-page :global(.notebook-entry.is-done .notebook-entry__title) {
    text-decoration: line-through;
    text-decoration-color: var(--color-sage);
    text-decoration-thickness: 1px;
    color: var(--notebook-ink-faint);
  }

  .tasks-page :global(.notebook-entry.is-low .notebook-entry__title) {
    color: var(--notebook-ink-faint);
  }

  .tasks-page :global(.notebook-entry__tags) {
    display: inline-flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.35rem;
    line-height: 1;
  }

  /*
    Priority + status read as small, quiet chips — consistent height and
    padding so the task title always stays the strongest element in the row.
  */
  .tasks-page :global(.task-badge) {
    display: inline-flex;
    align-items: center;
    height: 1.15rem;
    padding: 0 0.45rem;
    border: 1px solid transparent;
    border-radius: 999px;
    font-family: var(--font-body);
    font-size: 0.62rem;
    font-weight: 600;
    font-style: normal;
    line-height: 1;
    letter-spacing: var(--tracking-wide);
    text-transform: uppercase;
    white-space: nowrap;
    color: var(--notebook-ink-faint);
    background: var(--notebook-surface-muted);
  }

  /* High priority: ONE quiet red accent (badge only — no bar, no red check). */
  .tasks-page :global(.task-badge--high) {
    color: #9a4038;
    background: rgba(196, 90, 78, 0.1);
    border-color: rgba(196, 90, 78, 0.28);
  }

  .tasks-page :global(.task-badge--high::before) {
    content: "★";
    margin-right: 0.22rem;
    opacity: 0.8;
  }

  .tasks-page :global(.task-badge--low) {
    color: var(--notebook-ink-faint);
    background: transparent;
    border-color: var(--notebook-border);
  }

  .tasks-page :global(.task-badge--open) {
    color: #4f6b4a;
    background: rgba(141, 158, 136, 0.18);
    border-color: rgba(141, 158, 136, 0.4);
  }

  .tasks-page :global(.task-badge--claimed) {
    color: var(--notebook-ink-muted);
    background: var(--notebook-surface-muted);
    border-color: var(--notebook-border);
  }

  .tasks-page :global(.task-badge--done) {
    color: #4f6b4a;
    background: rgba(141, 158, 136, 0.14);
    border-color: rgba(141, 158, 136, 0.3);
  }

  .tasks-page :global(.task-badge--muted) {
    opacity: 0.7;
  }

  /* Due date is a colour-coded chip so overdue/soon are noticeable — sized to
     match the other badges so it never dominates the title. */
  .tasks-page :global(.task-due) {
    display: inline-flex;
    align-items: center;
    gap: 0.25rem;
    height: 1.15rem;
    padding: 0 0.5rem;
    border: 1px solid var(--notebook-border);
    border-radius: 999px;
    font-family: var(--font-body);
    font-size: 0.62rem;
    font-weight: 600;
    line-height: 1;
    letter-spacing: 0.02em;
    white-space: nowrap;
    color: var(--notebook-ink-muted);
    background: var(--notebook-surface);
  }

  .tasks-page :global(.task-due::before) {
    content: "◷";
    font-size: 0.72rem;
    line-height: 1;
  }

  .tasks-page :global(.task-due--soon) {
    color: #8a6314;
    background: rgba(231, 172, 70, 0.16);
    border-color: var(--color-citrine);
  }

  .tasks-page :global(.task-due--overdue) {
    color: #8f2a2a;
    background: rgba(196, 90, 78, 0.16);
    border-color: rgba(196, 90, 78, 0.5);
  }

  .tasks-page :global(.task-due--overdue::before) {
    content: "⚠";
  }

  .tasks-page :global(.notebook-entry__description) {
    margin: 0.1rem 0 0;
    padding-left: 0.05rem;
    font-family: var(--font-body);
    font-size: var(--text-sm);
    font-style: normal;
    line-height: 1.5;
    color: var(--notebook-ink-muted);
  }

  .tasks-page :global(.notebook-entry__meta) {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.15rem 0.6rem;
    margin-top: 0.25rem;
    font-family: var(--font-body);
    font-size: var(--text-xs);
    line-height: 1.5;
    color: var(--notebook-ink-faint);
  }

  .tasks-page :global(.notebook-entry__meta span:not(:first-child)::before) {
    content: "·";
    margin-right: 0.6rem;
    opacity: 0.65;
  }

  /*
    All actions travel together as one group beneath the details — Claim,
    Edit, Cancel side by side (primary → secondary → destructive), never a
    lone Cancel stranded at the far right.
  */
  .tasks-page :global(.notebook-entry__actions) {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.4rem 0.5rem;
    margin-top: calc(var(--notebook-line) * 0.3);
  }

  .tasks-page :global(.task-action) {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.42rem 0.85rem;
    border: 1px solid transparent;
    border-radius: var(--radius-sm);
    font-family: var(--font-body);
    font-size: 0.68rem;
    font-weight: 700;
    letter-spacing: var(--tracking-wide);
    text-transform: uppercase;
    line-height: 1;
    cursor: pointer;
    transition:
      background var(--transition-fast),
      border-color var(--transition-fast),
      box-shadow var(--transition-fast),
      color var(--transition-fast);
  }

  /* Primary = citrine (CLAIM / COMPLETE) — the fast, forward action. */
  .tasks-page :global(.task-action--primary) {
    background: var(--color-citrine);
    border-color: var(--color-citrine);
    color: var(--color-black);
  }

  .tasks-page :global(.task-action--primary:hover),
  .tasks-page :global(.task-action--primary:focus-visible) {
    background: var(--color-citrine-hover);
    border-color: var(--color-citrine-hover);
    outline: none;
    box-shadow: var(--shadow-sm), 0 0 0 3px var(--color-focus-ring);
  }

  /* Ghost = neutral outline (EDIT) — available but recessive. */
  .tasks-page :global(.task-action--ghost) {
    background: var(--notebook-surface);
    border-color: var(--notebook-border-strong);
    color: var(--notebook-ink);
  }

  .tasks-page :global(.task-action--ghost:hover),
  .tasks-page :global(.task-action--ghost:focus-visible) {
    background: var(--notebook-surface-hover);
    border-color: var(--notebook-ink-faint);
    outline: none;
    box-shadow: 0 0 0 3px var(--color-focus-ring);
  }

  /* Danger = quiet outline that reveals intent on hover (CANCEL). */
  .tasks-page :global(.task-action--danger) {
    background: transparent;
    border-color: var(--notebook-border-strong);
    color: var(--notebook-ink-muted);
  }

  .tasks-page :global(.task-action--danger:hover),
  .tasks-page :global(.task-action--danger:focus-visible) {
    background: rgba(196, 90, 78, 0.1);
    border-color: rgba(196, 90, 78, 0.55);
    color: #8f2a2a;
    outline: none;
    box-shadow: 0 0 0 3px rgba(196, 90, 78, 0.18);
  }

  .tasks-page :global(.notebook-entry__actions .task-action--danger) {
    grid-column: 1 / -1;
    margin-left: 0;
  }

  .tasks-page :global(.notebook-entry__actions .task-action--primary:only-of-type) {
    grid-column: 1 / -1;
  }

  @media (min-width: 48rem) {
    .notebook__paper {
      padding-right: 2.5rem;
    }

    .tasks-page :global(.notebook-entry__main) {
      flex-wrap: nowrap;
    }

    .tasks-page :global(.notebook-entry__tags) {
      margin-left: auto;
    }
  }

  /*
    Desktop sidebar polish — page-scoped value tweaks only (slightly narrower
    column, more compact nav rows). No structural change to the frozen global
    sidebar architecture.
  */
  @media (min-width: 901px) {
    .tasks-page :global(.team-list-layout__body) {
      grid-template-columns: minmax(184px, 200px) 1fr;
    }

    .tasks-page :global(.team-list-layout__main) {
      padding-inline: var(--space-5);
      padding-top: var(--space-4);
    }

    .tasks-page :global(.team-list-layout__sidebar-nav-btn) {
      padding-top: 0.4rem;
      padding-bottom: 0.4rem;
    }

    .tasks-page__action-long {
      display: inline;
    }

    .tasks-page__action-short {
      display: none;
    }

    .tasks-page__toolbar {
      display: flex;
    }

    .tasks-page :global(.team-list-layout__toolbar) {
      display: block;
    }

    .tasks-summary {
      display: flex;
    }
  }

  @media (min-width: 1200px) {
    .tasks-page__content {
      display: grid;
      grid-template-columns: minmax(0, 1fr) minmax(15rem, 17.5rem);
      align-items: start;
      gap: 1.25rem;
    }

    .tasks-page__secondary {
      display: flex;
    }

    .notebook {
      margin-inline: 0;
    }

    .tasks-tabs {
      margin-inline: 0;
    }
  }

  /* Mobile: compact tab row replaces sidebar nav. */
  @media (max-width: 900px) {
    .tasks-tabs {
      display: block;
    }

    .tasks-page :global(.team-list-layout__sidebar) {
      display: none;
    }

    .tasks-page :global(.team-list-layout__mobile-filter-bar) {
      display: none;
    }
  }

  @media (max-width: 640px) {
    .tasks-page {
      --notebook-line: 2rem;
      --notebook-margin: 1.65rem;
    }

    .tasks-page :global(.team-list-layout__main) {
      padding-inline: var(--shell-main-padding-x-compact);
      padding-top: var(--shell-main-padding-top-compact);
      padding-bottom: calc(var(--space-6) + env(safe-area-inset-bottom, 0px));
    }

    .tasks-page :global(.page-context-header__actions .team-list-layout__btn-primary) {
      flex: 1;
      min-height: 2.75rem;
    }

    .notebook {
      max-width: 100%;
      min-width: 0;
      overflow: hidden;
    }

    .notebook__binding {
      gap: clamp(1.5rem, 18vw, 3rem);
      padding-inline: 0.75rem;
    }

    .notebook__paper {
      padding: 0.85rem 0.65rem 1.25rem calc(var(--notebook-margin) + 0.55rem);
      min-height: 10rem;
      overflow-x: hidden;
    }

    .tasks-page :global(.notebook-entry__title) {
      font-size: var(--text-base);
      line-height: 1.35;
      overflow-wrap: anywhere;
    }

    .tasks-page :global(.notebook-entry__main) {
      flex-direction: column;
      align-items: flex-start;
      gap: 0.35rem;
      min-height: auto;
    }

    .tasks-page :global(.notebook-entry__tags) {
      width: 100%;
    }

    .tasks-page :global(.notebook-entry__meta span) {
      overflow-wrap: anywhere;
    }

    .tasks-page :global(.notebook-entry__actions) {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 0.45rem;
      width: 100%;
    }

    .tasks-page :global(.notebook-entry__actions .task-action--danger) {
      grid-column: 1 / -1;
      margin-left: 0;
    }

    .tasks-page :global(.notebook-entry__actions .task-action--primary:only-of-type) {
      grid-column: 1 / -1;
    }

    .tasks-page :global(.task-action) {
      width: 100%;
      min-height: 2.75rem;
      padding-inline: 0.5rem;
      font-size: 0.62rem;
    }

    .tasks-modal__form .form-stack,
    .tasks-modal__header,
    .tasks-modal__footer {
      padding-inline: var(--space-4);
    }
  }

  @media (max-width: 380px) {
    .tasks-page {
      --notebook-margin: 1.35rem;
      --notebook-line: 1.85rem;
    }

    .tasks-tabs__long {
      display: none;
    }

    .tasks-tabs__short {
      display: inline;
    }

    .tasks-tabs__btn {
      min-height: 2rem;
      padding-inline: 0.55rem;
      font-size: 0.75rem;
    }
  }

  .tasks-modal {
    width: min(32rem, calc(100vw - 2rem));
    max-height: calc(100vh - 2rem);
    padding: 0;
    border: 1px solid var(--color-stone-200);
    border-radius: var(--radius-md);
    background: var(--color-bg);
    box-shadow: var(--shadow-lg);
  }

  .tasks-modal--narrow {
    width: min(26rem, calc(100vw - 2rem));
  }

  .tasks-modal::backdrop {
    background: rgba(47, 42, 40, 0.4);
  }

  .tasks-modal__form {
    display: grid;
    gap: 0;
    padding: 0;
    margin: 0;
  }

  .tasks-modal__form .form-stack {
    margin-top: 0;
    padding: var(--space-4) var(--space-5);
    gap: var(--space-4);
  }

  .tasks-modal__header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-3);
    padding: var(--space-4) var(--space-5);
    border-bottom: 1px solid var(--color-stone-200);
    background: var(--color-stone-50);
  }

  .tasks-modal__header h2 {
    margin: 0;
    font-family: var(--font-display);
    font-size: var(--text-xl);
    font-weight: 500;
    line-height: var(--leading-tight);
    color: var(--color-text);
  }

  .tasks-modal__close {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2rem;
    height: 2rem;
    border: none;
    border-radius: var(--radius-sm);
    background: none;
    font-size: 1.35rem;
    line-height: 1;
    color: var(--color-stone-500);
    cursor: pointer;
    transition:
      background var(--transition-fast),
      color var(--transition-fast);
  }

  .tasks-modal__close:hover {
    background: var(--color-stone-100);
    color: var(--color-text);
  }

  .tasks-modal__close:focus-visible {
    outline: 2px solid var(--color-citrine);
    outline-offset: 2px;
  }

  .tasks-modal__fieldset {
    margin: 0;
    padding: 0;
    border: none;
    display: grid;
    gap: var(--space-2);
  }

  .tasks-modal__radio {
    display: flex;
    align-items: baseline;
    gap: var(--space-2);
    font-family: var(--font-body);
    font-size: var(--text-sm);
    text-transform: none;
    letter-spacing: normal;
    cursor: pointer;
  }

  .tasks-modal__radio strong {
    font-weight: 600;
  }

  .tasks-modal__hint {
    margin: 0;
    font-family: var(--font-body);
    font-size: var(--text-xs);
    font-weight: 400;
    text-transform: none;
    letter-spacing: normal;
    line-height: var(--leading-normal);
    color: var(--color-stone-500);
  }

  .tasks-modal__assignees {
    display: grid;
    gap: var(--space-2);
  }

  .tasks-modal__staff-list {
    display: grid;
    gap: var(--space-1);
    max-height: 10rem;
    overflow-y: auto;
    padding: var(--space-2);
    border: 1px solid var(--color-stone-200);
    border-radius: var(--radius-sm);
    background: var(--color-stone-50);
  }

  .tasks-modal__staff-option {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    font-family: var(--font-body);
    font-size: var(--text-sm);
    text-transform: none;
    letter-spacing: normal;
    cursor: pointer;
  }

  .tasks-modal__staff-option input {
    accent-color: var(--color-citrine);
  }

  .tasks-modal__footer {
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-end;
    gap: var(--space-2);
    padding: var(--space-4) var(--space-5);
    border-top: 1px solid var(--color-stone-200);
    background: var(--color-stone-50);
  }

  .tasks-modal__lead {
    margin: 0;
    font-family: var(--font-display);
    font-size: var(--text-lg);
    font-weight: 500;
    line-height: var(--leading-normal);
    color: var(--color-stone-700);
  }
</style>
