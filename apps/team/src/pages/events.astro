---
import TeamLayout from "../layouts/TeamLayout.astro";
import TeamPage from "../components/TeamPage.astro";
import { isSalonManager } from "../lib/auth";
import { teamUrl } from "../lib/supabase-server";

const { supabase, staff } = Astro.locals;

if (!staff) {
  return Astro.redirect(teamUrl("/login?error=unlinked"));
}

const managerView = isSalonManager(staff);

const { data: staffRows } = await supabase
  .from("staff")
  .select("id, name")
  .order("name");

const staffOptions = staffRows ?? [];
---

<TeamLayout title="Events" activeNav="events" plain>
  <div
    class="events-page"
    data-events-app
    data-api-base={teamUrl("/api/events")}
    data-manager={managerView ? "1" : "0"}
    data-current-staff-id={staff.id}
  >
    <TeamPage
      title="Team calendar"
      lead="Birthdays, time off, closures, and salon announcements — not the appointment book."
      toolbar
    >
      <button slot="actions" class="btn-primary" type="button" data-event-create>
        Add event
      </button>

      <div slot="toolbar" class="events-page__toolbar">
        <button class="btn-secondary events-page__nav-btn" type="button" data-month-prev aria-label="Previous month">
          ‹
        </button>
        <h2 class="events-page__month" data-month-label></h2>
        <button class="btn-secondary events-page__nav-btn" type="button" data-month-next aria-label="Next month">
          ›
        </button>
        <button class="btn-secondary events-page__today" type="button" data-month-today>
          Today
        </button>
      </div>

      <p class="notice notice--error events-page__error" data-events-error hidden role="alert"></p>

      <section class="events-calendar-panel" aria-label="Month calendar">
        <div class="events-calendar" data-events-calendar></div>
        <div class="events-calendar-legend" aria-hidden="true">
          <span class="events-calendar-legend__item events-calendar-legend__item--closure">Holiday / closure</span>
          <span class="events-calendar-legend__item events-calendar-legend__item--event">Community</span>
          <span class="events-calendar-legend__item events-calendar-legend__item--announcement">Announcement</span>
          <span class="events-calendar-legend__item events-calendar-legend__item--time_off">Time off (staff color)</span>
        </div>
      </section>

      <section class="events-upcoming team-panel">
        <h3 class="team-panel__title">Upcoming</h3>
        <div class="events-list" data-events-list aria-live="polite">
          <p class="empty-state">Loading events…</p>
        </div>
      </section>
    </TeamPage>
  </div>

  <dialog class="events-modal" data-event-modal aria-labelledby="event-modal-title">
    <form class="events-modal__form" method="dialog" data-event-form>
      <header class="events-modal__header">
        <h2 id="event-modal-title" data-event-modal-title>Add event</h2>
        <button class="events-modal__close" type="button" data-event-modal-close aria-label="Close">
          ×
        </button>
      </header>

      <div class="form-stack">
        <label class="form-label">
          Title
          <input class="form-input" name="title" type="text" maxlength="200" required />
        </label>

        <label class="form-label">
          Description (optional)
          <textarea class="form-input" name="description" rows="3" maxlength="2000"></textarea>
        </label>

        <label class="form-label">
          Type
          <select class="form-input" name="event_type" data-event-type>
            {
              managerView ? (
                <>
                  <option value="event">Community event</option>
                  <option value="announcement">Salon announcement</option>
                  <option value="closure">Holiday / closure</option>
                  <option value="time_off">Time off</option>
                </>
              ) : (
                <option value="time_off">Time off (my schedule)</option>
              )
            }
          </select>
        </label>

        <div class="events-modal__staff" data-staff-picker hidden>
          <label class="form-label">
            Team member
            <select class="form-input" name="staff_id">
              {
                staffOptions.map((member) => (
                  <option value={member.id} selected={member.id === staff.id}>
                    {member.name}
                  </option>
                ))
              }
            </select>
          </label>
        </div>

        <label class="events-modal__checkbox">
          <input type="checkbox" name="all_day" data-all-day />
          All day
        </label>

        <label class="form-label" data-start-label>
          Starts
          <input class="form-input" name="starts_at" type="datetime-local" required />
        </label>

        <label class="form-label" data-end-label>
          Ends (optional)
          <input class="form-input" name="ends_at" type="datetime-local" />
        </label>
      </div>

      <footer class="events-modal__footer">
        <button class="btn-secondary" type="button" data-event-modal-close>Cancel</button>
        {
          managerView ? (
            <button class="btn-secondary" type="button" data-event-delete hidden>
              Delete
            </button>
          ) : (
            <button class="btn-secondary" type="button" data-event-delete hidden>
              Remove
            </button>
          )
        }
        <button class="btn-primary" type="submit" data-event-submit>Save</button>
      </footer>
    </form>
  </dialog>
</TeamLayout>

<script src="../scripts/events.ts"></script>

<style>
  .events-page__toolbar {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.5rem;
    width: 100%;
  }

  .events-page__nav-btn {
    min-width: 2.25rem;
    padding-inline: 0.65rem;
    font-size: 1.1rem;
    line-height: 1;
  }

  .events-page__month {
    margin: 0;
    flex: 1;
    min-width: 10rem;
    text-align: center;
    font-family: var(--font-sans);
    font-size: 0.95rem;
    font-weight: 600;
    letter-spacing: 0.02em;
  }

  .events-page__today {
    margin-left: auto;
  }

  .events-page__error {
    margin-top: 0;
    margin-bottom: 1rem;
  }

  .events-calendar-panel {
    margin-bottom: 1rem;
    border: 1px solid var(--color-border-subtle);
    border-radius: 10px;
    background: var(--color-bg);
    box-shadow: 0 2px 16px color-mix(in srgb, var(--color-charcoal) 6%, transparent);
    overflow: hidden;
  }

  .events-calendar {
    display: grid;
    grid-template-columns: repeat(7, minmax(0, 1fr));
    gap: 0;
    background: var(--color-border-subtle);
    border-bottom: 1px solid var(--color-border-subtle);
  }

  .events-calendar__head,
  .events-calendar__cell {
    background: var(--color-bg);
    font-family: var(--font-sans);
  }

  .events-calendar__head {
    padding: 0.5rem 0.25rem;
    text-align: center;
    font-size: 0.625rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--color-gray);
    background: color-mix(in srgb, var(--color-cream) 65%, var(--color-bg));
    border-bottom: 1px solid var(--color-border-subtle);
  }

  .events-calendar__cell {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 0.2rem;
    min-height: 4.75rem;
    padding: 0.35rem 0.4rem 0.3rem;
    cursor: default;
    border-right: 1px solid var(--color-border-subtle);
    border-bottom: 1px solid var(--color-border-subtle);
    transition:
      background 0.15s ease,
      box-shadow 0.15s ease;
  }

  .events-calendar__cell:nth-child(7n) {
    border-right: none;
  }

  .events-calendar__cell:hover:not(.events-calendar__cell--pad) {
    background: color-mix(in srgb, var(--color-sage) 8%, var(--color-bg));
    box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--color-sage) 35%, transparent);
    z-index: 1;
  }

  .events-calendar__cell--pad {
    background: color-mix(in srgb, var(--color-cream) 50%, var(--color-bg));
    opacity: 0.55;
  }

  .events-calendar__cell--weekend:not(.events-calendar__cell--pad) {
    background: color-mix(in srgb, var(--color-cream) 35%, var(--color-bg));
  }

  .events-calendar__cell--today {
    background: color-mix(in srgb, var(--color-dusty-rose) 6%, var(--color-bg));
    box-shadow: inset 0 0 0 2px color-mix(in srgb, var(--color-dusty-rose) 45%, transparent);
  }

  .events-calendar__cell--today:hover {
    background: color-mix(in srgb, var(--color-dusty-rose) 10%, var(--color-bg));
  }

  .events-calendar__cell--today .events-calendar__day {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 1.45rem;
    height: 1.45rem;
    padding: 0 0.2rem;
    border-radius: 999px;
    background: var(--color-dusty-rose);
    color: #fff;
    font-weight: 700;
  }

  .events-calendar__cell--closure {
    background: color-mix(in srgb, #c83c3c 9%, var(--color-bg));
  }

  .events-calendar__cell--closure.events-calendar__cell--weekend {
    background: color-mix(in srgb, #c83c3c 11%, color-mix(in srgb, var(--color-cream) 35%, var(--color-bg)));
  }

  .events-calendar__cell--event {
    background: color-mix(in srgb, var(--color-citrine) 12%, var(--color-bg));
  }

  .events-calendar__cell--announcement {
    background: color-mix(in srgb, #8a7355 10%, var(--color-bg));
  }

  .events-calendar__day {
    font-size: 0.72rem;
    font-weight: 600;
    line-height: 1;
    color: var(--color-text);
  }

  .events-calendar__cell--pad .events-calendar__day {
    visibility: hidden;
  }

  .events-calendar__markers {
    display: flex;
    flex-direction: column;
    gap: 0.15rem;
    width: 100%;
    min-height: 0;
    flex: 1;
  }

  .events-calendar__pill {
    display: block;
    width: 100%;
    padding: 0.1rem 0.35rem;
    border-radius: 3px;
    border-left: 2px solid var(--event-staff-color, var(--color-dusty-rose));
    background: color-mix(in srgb, var(--event-staff-color, var(--color-dusty-rose)) 12%, transparent);
    font-size: 0.5625rem;
    font-weight: 600;
    line-height: 1.3;
    letter-spacing: 0.01em;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .events-calendar__pill--closure {
    border-left-color: #c83c3c;
    background: color-mix(in srgb, #c83c3c 14%, transparent);
    color: #7a2020;
  }

  .events-calendar__pill--announcement {
    border-left-color: #8a7355;
    background: color-mix(in srgb, #8a7355 14%, transparent);
    color: #5c4a32;
  }

  .events-calendar__pill--event {
    border-left-color: var(--color-citrine);
    background: color-mix(in srgb, var(--color-citrine) 16%, transparent);
    color: var(--color-text);
  }

  .events-calendar__dots {
    display: flex;
    flex-wrap: wrap;
    gap: 0.2rem;
    align-items: center;
    margin-top: auto;
  }

  .events-calendar__dot {
    display: block;
    width: 0.4rem;
    height: 0.4rem;
    border-radius: 50%;
    background: var(--event-staff-color, var(--color-dusty-rose));
    flex-shrink: 0;
    box-shadow: 0 0 0 1px color-mix(in srgb, var(--color-bg) 70%, transparent);
  }

  .events-calendar__more {
    font-size: 0.5625rem;
    font-weight: 700;
    letter-spacing: 0.03em;
    color: var(--color-gray);
    line-height: 1;
  }

  .events-calendar-legend {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem 1.25rem;
    padding: 0.5rem 0.9rem;
    border-top: 1px solid var(--color-border-subtle);
    background: color-mix(in srgb, var(--color-cream) 40%, var(--color-bg));
  }

  .events-calendar-legend__item {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    font-family: var(--font-sans);
    font-size: 0.6rem;
    font-weight: 500;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    color: var(--color-gray);
  }

  .events-calendar-legend__item::before {
    content: "";
    width: 0.55rem;
    height: 0.55rem;
    border-radius: 2px;
    flex-shrink: 0;
  }

  .events-calendar-legend__item--closure::before {
    background: color-mix(in srgb, #c83c3c 35%, transparent);
    border: 1px solid color-mix(in srgb, #c83c3c 55%, transparent);
  }

  .events-calendar-legend__item--event::before {
    background: color-mix(in srgb, var(--color-citrine) 40%, transparent);
    border: 1px solid color-mix(in srgb, var(--color-citrine) 60%, transparent);
  }

  .events-calendar-legend__item--announcement::before {
    background: color-mix(in srgb, #8a7355 30%, transparent);
    border: 1px solid color-mix(in srgb, #8a7355 50%, transparent);
  }

  .events-calendar-legend__item--time_off::before {
    background: linear-gradient(
      90deg,
      var(--color-citrine) 0 33%,
      var(--color-sage) 33% 66%,
      #d4a5a5 66% 100%
    );
    border-radius: 999px;
  }

  .events-upcoming {
    margin-top: 0;
  }

  .events-upcoming .team-panel__title {
    font-size: 0.8rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    margin-bottom: 0.5rem;
  }

  .events-list {
    display: flex;
    flex-direction: column;
    border: 1px solid var(--color-border-subtle);
    border-radius: 8px;
    overflow: hidden;
    background: var(--color-bg);
  }

  .event-row {
    display: grid;
    grid-template-columns: auto 1fr auto;
    grid-template-rows: auto auto;
    grid-template-areas:
      "dot title badge"
      "dot when staff";
    align-items: center;
    column-gap: 0.55rem;
    row-gap: 0.1rem;
    width: 100%;
    padding: 0.45rem 0.65rem;
    border: none;
    border-bottom: 1px solid var(--color-border-subtle);
    background: var(--color-bg);
    cursor: pointer;
    text-align: left;
    font: inherit;
    color: inherit;
    transition: background 0.12s ease;
  }

  .event-row:last-child {
    border-bottom: none;
  }

  .event-row:hover,
  .event-row:focus-visible {
    background: color-mix(in srgb, var(--color-sage) 10%, var(--color-bg));
    outline: none;
  }

  .event-row__dot {
    grid-area: dot;
    align-self: center;
    width: 0.45rem;
    height: 0.45rem;
    border-radius: 50%;
    background: var(--event-staff-color, var(--color-dusty-rose));
    flex-shrink: 0;
  }

  .event-row--closure .event-row__dot {
    background: #c83c3c;
  }

  .event-row--announcement .event-row__dot {
    background: #8a7355;
  }

  .event-row__title {
    grid-area: title;
    min-width: 0;
    font-size: 0.8125rem;
    font-weight: 600;
    line-height: 1.25;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .event-row__when {
    grid-area: when;
    font-family: var(--font-sans);
    font-size: 0.6875rem;
    line-height: 1.2;
    color: var(--color-gray);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .event-row__staff {
    grid-area: staff;
    justify-self: end;
    font-family: var(--font-sans);
    font-size: 0.625rem;
    color: var(--color-gray);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    max-width: 8rem;
  }

  .event-row .event-badge {
    grid-area: badge;
    justify-self: end;
    align-self: start;
    padding: 0.1rem 0.4rem;
    font-size: 0.5625rem;
    letter-spacing: 0.05em;
  }

  .event-badge {
    display: inline-flex;
    align-items: center;
    padding: 0.18rem 0.5rem;
    border: 1px solid var(--color-border);
    border-radius: 999px;
    font-family: var(--font-sans);
    font-size: 0.58rem;
    font-weight: 600;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    white-space: nowrap;
  }

  .event-badge--time_off {
    border-color: color-mix(in srgb, var(--event-staff-color, #7a8f6e) 55%, var(--color-border));
    color: var(--color-text);
    background: color-mix(in srgb, var(--event-staff-color, #7a8f6e) 12%, transparent);
  }

  .event-badge--closure {
    border-color: #c83c3c;
    color: #922;
    background: rgba(200, 60, 60, 0.1);
  }

  .event-badge--announcement {
    border-color: #8a7355;
    color: #8a7355;
    background: rgba(138, 115, 85, 0.1);
  }

  .event-badge--event {
    border-color: var(--color-citrine);
    color: var(--color-text);
    background: rgba(196, 168, 106, 0.14);
  }

  .events-modal {
    width: min(32rem, calc(100vw - 2rem));
    max-height: calc(100vh - 2rem);
    padding: 0;
    border: 1px solid var(--color-border);
    background: var(--color-bg);
    box-shadow: 0 12px 48px var(--color-card-shadow);
  }

  .events-modal::backdrop {
    background: rgba(0, 0, 0, 0.35);
  }

  .events-modal__form {
    display: grid;
    gap: 1rem;
    padding: 1.25rem;
    margin: 0;
  }

  .events-modal__header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.75rem;
  }

  .events-modal__header h2 {
    margin: 0;
    font-size: 1.15rem;
  }

  .events-modal__close {
    border: none;
    background: none;
    font-size: 1.5rem;
    line-height: 1;
    color: var(--color-gray);
    cursor: pointer;
  }

  .events-modal__checkbox {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.95rem;
    text-transform: none;
    letter-spacing: normal;
    cursor: pointer;
  }

  .events-modal__footer {
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-end;
    gap: 0.5rem;
    padding-top: 0.25rem;
  }

  @media (max-width: 640px) {
    .events-calendar__cell {
      min-height: 3.5rem;
      padding: 0.25rem 0.2rem 0.2rem;
    }

    .events-calendar__day {
      font-size: 0.65rem;
    }

    .events-calendar__cell--today .events-calendar__day {
      min-width: 1.25rem;
      height: 1.25rem;
    }

    .events-calendar__pill {
      display: none;
    }

    .events-calendar__dot {
      width: 0.35rem;
      height: 0.35rem;
    }

    .events-calendar__head {
      font-size: 0.5625rem;
      padding: 0.35rem 0.1rem;
    }

    .events-calendar-legend {
      gap: 0.4rem 0.75rem;
      padding: 0.4rem 0.6rem;
    }

    .events-calendar-legend__item {
      font-size: 0.5625rem;
    }

    .event-row {
      padding: 0.4rem 0.5rem;
      column-gap: 0.45rem;
    }

    .event-row__title {
      font-size: 0.75rem;
    }

    .event-row__when {
      font-size: 0.625rem;
    }

    .event-row__staff {
      max-width: 4.5rem;
      font-size: 0.5625rem;
    }

    .event-row .event-badge {
      padding: 0.08rem 0.32rem;
      font-size: 0.5rem;
    }
  }
</style>
